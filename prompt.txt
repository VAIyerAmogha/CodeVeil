Read CLAUDE.md and Plan.md fully before doing anything.

Task: Frontend scaffold

1. Initialize Next.js (if not already done):
   npx create-next-app@latest frontend --typescript --tailwind --app --eslint
   Then restructure to match the src/ layout in CLAUDE.md

2. frontend/tailwind.config.ts
   - darkMode: "class"
   - Extend colors:
       primary: "#2563EB"
       success: "#22C55E"
       warning: "#F59E0B"
   - fontFamily: inter: ["Inter", "sans-serif"]

3. frontend/src/app/layout.tsx
   - Import Inter from next/font/google
   - Apply Inter to <html>
   - <html> has className="dark"
   - <body> has black background and white text

4. frontend/src/components/layout/Sidebar.tsx
   - Fixed left rail, dark bg
   - Nav links: Dashboard (/dashboard), Profile (/profile)
   - "CodeVeil" logo text in #2563EB at top

5. frontend/src/components/layout/Navbar.tsx
   - Top bar, dark bg
   - Right side: user avatar circle placeholder

6. frontend/src/components/layout/PageWrapper.tsx
   - Sidebar left + Navbar top + children fills remaining space

7. frontend/src/store/authStore.ts
   - Zustand: { user: null, token: null, login: () => {}, logout: () => {} }

8. frontend/src/store/repoStore.ts
   - Zustand: { currentRepoId: null, jobId: null, setRepo: () => {}, setJob: () => {} }

9. frontend/.env.local
   NEXT_PUBLIC_API_URL=http://localhost:8000

Rules:
- No TypeScript any types
- No hardcoded data
- Install zustand: npm install zustand

After implementing:
- Run npm run build — must complete with 0 errors
- Fix all TypeScript errors before reporting done

Then update the CLAUDE.md file as per the rules:
- Current task → 1.4 complete, Phase 1 done, next is Phase 2 Task 2.1
- Add to Recent completions: all frontend files created
- Decisions log: Next.js 14 App Router with dark mode class strategy